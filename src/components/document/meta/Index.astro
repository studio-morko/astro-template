---
import { Env } from '@lib/env';

interface Props {
  value: boolean;
}

let { 
  value
} = Astro.props;

if (Env.is("PUBLIC_ENVIRONMENT", "development")) {
  value = false;
}

---

<meta name="robots" content={value ? 'index' : 'noindex'} /> 